name: reduced_fluttertriple
description: reduced for fluttertriple.
version: 0.1.0
homepage:
publish_to: none

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  reduced:
    git:
      url: https://github.com/partmaster/reduced.git
      ref: main
  flutter_triple: ^1.2.7+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
